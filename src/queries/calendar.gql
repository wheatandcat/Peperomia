query Calendar($date: String!) {
  calendar(date: $date) {
    id
    date
    item {
      id
      title
      kind
      itemDetails {
        id
        title
        kind
        memo
        moveMinutes
        url
        place
        priority
      }
    }
  }
}
